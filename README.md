### Team EKL Website

#### How to add a flyer
- Check `flyers/`. Name the new flyer `n.png` where `n` is the highest number in this directory. Upload the flyer into `flyers/`. 
- Add flyer name to `flyers/index.json`. Don't forget to add a comma after the flyer name. 
The rest is done automatically :-)
